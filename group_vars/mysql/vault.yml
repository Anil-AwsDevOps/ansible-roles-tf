
MYSQL_ROOT_PASSWORD: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/{{ env }}/mysql', region='us-east-1', decrypt=True) }}"

#"{{ (lookup('amazon.aws.aws_secret', 'prod/roboshop/mysql',region='us-east-1') |  from_json).MYSQL_ROOT_PASSWORD }}"

# 
